# Runtime data — never commit these
config/*.db
config/*.log
config/plugins/
config/logs/
# Ignore user-created bot instances but keep the shipped default
config/bots/*
!config/bots/default/

# Local configuration overrides
docker-compose.override.yml
.env
.env.local

# Editor files
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/launch.json
